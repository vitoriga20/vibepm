[package]
name = "vibepm-desktop"
version = "0.1.0"
description = "vibepm desktop shell"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-single-instance = "2"

[profile.release]
strip = true
lto = true
codegen-units = 1
