{
  "name": "@vibepm/plugin-storage",
  "version": "0.1.0",
  "description": "本地 SQLite 数据库服务（db）：设置/项目/同步元数据存取。",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "vibepm": {
    "node": {
      "inject": [],
      "provide": ["db"]
    }
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"import('fs').then(f=>f.rmSync('dist',{recursive:true,force:true}))\"",
    "test": "node --import tsx --test test/**/*.test.ts || exit 0"
  },
  "dependencies": {
    "@vibepm/core": "workspace:*",
    "@vibepm/plugin-web-ui": "workspace:*"
  }
}
