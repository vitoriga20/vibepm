{
  "name": "@vitoriga20/plugin-todo-timer",
  "version": "0.1.0",
  "description": "TODO番茄钟：番茄钟 + ToDo 小目标管理（专注计分、跨天归档、统计日历）。页面整体 iframe 嵌入预览壳，最大化复用自研页面。",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": ["dist", "client-dist"],
  "engines": {
    "node": ">=22.5.0"
  },
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    },
    "./client": {
      "types": "./dist/client/index.d.ts",
      "default": "./dist/client/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "clean": "rimraf dist client-dist",
    "build:ts": "tsc -b",
    "build:client": "esbuild client/index.ts --bundle --format=esm --outfile=client-dist/client.js --platform=browser --external:@vitoriga20/* && node scripts/copy-preview.mjs",
    "build": "pnpm run clean && pnpm run build:ts && pnpm run build:client",
    "dev": "pnpm run build"
  },
  "dependencies": {
    "@vitoriga20/core": "workspace:*",
    "@vitoriga20/plugin-ide-view": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "esbuild": "^0.24.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "vibepm": {
    "id": "plugin-todo-timer",
    "display": "TODO番茄钟",
    "role": "ui",
    "order": 40,
    "node": {
      "immediately": true,
      "inject": ["slots"],
      "provide": []
    },
    "client": {
      "entry": "./client-dist/client.js",
      "inject": [],
      "provide": [],
      "immediately": true
    },
    "configSchema": {}
  }
}
